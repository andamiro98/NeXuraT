spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10GB
      max-request-size: 10GB
      file-size-threshold: 2MB

ifc:
  storage:
    base-path: ./ifc-storage
  converter:
    url: http://localhost:3001
    chunk-target-mb: 800
